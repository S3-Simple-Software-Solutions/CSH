services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: herediano
      POSTGRES_PASSWORD: herediano2026
      POSTGRES_DB: herediano
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
