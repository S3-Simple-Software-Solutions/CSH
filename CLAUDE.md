# Claude Code Instructions

Use `AGENTS.md` and `docs/harness.md` as the source of truth for repository workflow.

This file exists because Claude Code automatically reads `CLAUDE.md` at session start. Keep detailed process rules in `docs/harness.md` so agent instructions stay consistent across tools.
